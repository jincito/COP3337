Extra Credit:
1) Exclude the mappings that map multiple letters to the same digit,
10% extra credit (110%):

FIND CODE: lines 138~ private static boolean isDigitAssigned(int[] digitValues, int i)
           line 114~ implements method isDigitAssigned in if-statement

2) Exclude the mappings that assign zero to letters that
are initial letters of the operands or the result,
another 10% extra credit (120%):

FIND CODE: line 146~ private static boolean isLeadingDigit(char variable)
           line 117~ implements method isLeadingDigit in if-statement